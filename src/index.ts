const message = document.getElementById("message");

if (message) {
  message.innerHTML =
    "A simple starter project that uses Parcel, TypeScript, ESlint, and Prettier.";
}

console.log("Hello world!");

export {};
